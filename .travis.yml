language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-extjs' ]; then cd .. && eval "mv $currentfolder generator-extjs" && cd generator-extjs; fi

